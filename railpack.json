{
    "$schema": "https://schema.railpack.com",
    "steps": {
        "extensions": {
            "commands": [
                "install-php-extensions ctype curl dom fileinfo filter hash mbstring openssl pcre pdo session tokenizer xml pdo_mysql redis intl zip exif gd"
            ]
        }
    }
}
